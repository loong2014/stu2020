package com.sunny.lib.image;

/**
 * https://www.fresco-cn.org/
 * <p>
 * 特别注意：Fresco 不支持 相对路径的URI. 所有的 URI 都必须是绝对路径，并且带上该 URI 的 scheme。
 * <p>
 * 使用SimpleDraweeView时，必须声明 android:layout_width 和 android:layout_height。如果没有在XML中声明这两个属性，将无法正确加载图像。
 */
public class FrescoImageConfig extends ImageConfig {

}
